# Demo screenshots

- `dashboard-hero.png` — the packaged JAR's public landing view.
- `verified-receipt.png` — the packaged JAR after the robust bundled candidate
  passed all nine evidence stages.
- `receipt-limitations.png` — the canonical limitations shown at the top of
  the same verified HTML receipt.

These are local final-build captures. Replace or supplement them with
signed-out public-deployment captures after Railway deployment. Do not edit a
local URL, duration, or verdict into a public screenshot.
